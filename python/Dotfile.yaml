# yaml-language-server: $schema=https://raw.githubusercontent.com/jlrickert/dots/main/schemas/dotfile.json
package:
  name: python
  description: "Python 3 runtime and uv package manager (brew on darwin, apt + Astral installer on Ubuntu) for dots packages that need scripting"
  version: 0.2.0
  requires:
    - "@self/common-shell"
  tags:
    - language
    - python
  platforms:
    - darwin
    - linux

links: {}

hooks:
  post_install: hooks/install-python.sh
