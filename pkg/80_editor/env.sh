have vi && export EDITOR=vi
have vim && export EDITOR=vim
have nvim && export EDITOR=nvim
have vi && export VISUAL=vi
have vim && export VISUAL=vim
have nvim && export VISUAL=nvim
have vi && export EDITOR_PREFIX=vi
have vim && export EDITOR_PREFIX=vim
have nvim && export EDITOR_PREFIX=nvim

export GIT_EDITOR="${EDITOR:-}"
