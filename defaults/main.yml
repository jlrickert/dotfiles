---
install_zsh: yes
install_emacs: yes
install_nvim: yes
install_vim: yes
data_dir: "{{ ansible_env.XDG_DATA_HOME|default('$HOME/.local/share') }}"
config_dir: "{{ ansible_env.XDG_CONFIG_HOME|default('$HOME/.config') }}"
cache_dir: "{{ ansible_env.XDG_CACHE_HOME|default('$HOME/.cache') }}"
