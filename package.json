{
	"name": "dotfiles",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"commit": "changeset",
		"changeset": "changeset",
		"prettier": "prettier --write \"**/*.{md,yaml,json}\"",
		"release": "changeset publish",
		"version-packages": "changeset version && npm run prettier",
		"publish": "changeset tag"
	},
	"devDependencies": {
		"@changesets/cli": "^2.27.7",
		"prettier": "^3.3.3"
	}
}
