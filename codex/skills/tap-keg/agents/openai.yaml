interface:
  display_name: "Tap KEG Notes"
  short_description: "Query and maintain KEG notes with tap CLI"
  default_prompt: "Use $tap-keg to inspect notes, tags, and node details in a KEG using tap."
